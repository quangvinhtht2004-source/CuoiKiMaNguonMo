# Bật module Rewriting (Thường cần thiết cho các hosting)
Options +FollowSymLinks
RewriteEngine On

# ----------------------------------------------------
# KHẮC PHỤC CACHING: BUỘC TRÌNH DUYỆT TẢI LẠI MỌI THỨ
# ----------------------------------------------------

<IfModule mod_headers.c>
    # Ngăn chặn caching mạnh mẽ cho các file web động và tĩnh
    <filesMatch "\.(html|htm|php|js|css|json)$">
        FileETag None
        # Vô hiệu hóa caching của trình duyệt và proxy
        Header set Cache-Control "max-age=0, no-cache, no-store, must-revalidate"
        Header set Pragma "no-cache"
        Header set Expires "0"
    </filesMatch>
</IfModule>